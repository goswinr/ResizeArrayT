{
  "type": "module",
  "scripts": {
    "watchTS": "dotnet fable watch --outDir _ts --lang typescript ../Src/ResizeArrayT.fsproj --run tsc",
    "buildTS": "dotnet fable --outDir _ts --noCache  --lang typescript ../Src/ResizeArrayT.fsproj --run tsc",
    "pretest": "dotnet fable --outDir _js --noCache ",
    "test": "mocha _js && npm run buildTS"
  },
  "devDependencies": {
    "mocha": "^11.7.5",
    "typescript": "^6.0.2"
  }
}

